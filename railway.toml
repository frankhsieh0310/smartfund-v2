[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c \"npx prisma migrate deploy && node --experimental-strip-types scripts/data/production/run-production-cron.ts\""
restartPolicyType = "NEVER"
cronSchedule = "*/5 * * * *"
