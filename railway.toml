[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c \"npx prisma migrate deploy && node --experimental-strip-types scripts/data/production/run-production-yahoo-stock-latest.ts\""
cronSchedule = "*/5 * * * *"

