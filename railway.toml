[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node --experimental-strip-types scripts/data/daily/run-production-yahoo-daily.ts --all-due"
restartPolicyType = "NEVER"
cronSchedule = "*/5 * * * *"
