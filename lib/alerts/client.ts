export type AlertRuleInput={scopeType?:'SINGLE_ASSET'|'WATCHLIST'|'CROSS_ASSET';watchlistId?:string;ruleFamily:string;ruleKind?:string;frequency:'ONE_TIME'|'RECURRING';cadence:string;cooldownSeconds?:number;deliveryChannels:string[];parameters:{logic:'AND'|'OR';conditions:Array<{id:string;assetId:string;assetType:string;metric:string;operator:string;threshold?:number}>}};
async function call<T>(method:string,body?:unknown,path='/api/alerts'){const response=await fetch(path,{method,credentials:'include',headers:body?{'Content-Type':'application/json'}:undefined,body:body?JSON.stringify(body):undefined});const payload=await response.json();if(!response.ok)throw new Error(payload.error?.message??'Alert request failed.');return payload.data as T}
export const listAlerts=()=>call<unknown[]>('GET'); export const createAlert=(input:AlertRuleInput)=>call<unknown>('POST',input); export const updateAlert=(input:unknown)=>call<unknown>('PATCH',input); export const deleteAlert=(id:string)=>call<{deleted:true}>('DELETE',{id}); export const alertHistory=(id:string)=>call<unknown[]>('GET',undefined,`/api/alerts?historyFor=${encodeURIComponent(id)}`);
